no_tty: true

pre-commit:
  jobs:
    - name: nano-staged
      run: ./node_modules/.bin/nano-staged --quiet

commit-msg:
  jobs:
    - name: commitlint
      run: ./node_modules/.bin/commitlint --edit {1}
