#!/usr/bin/env node

// Placeholder dispatcher. Real implementation resolves and execs the
// matching @julianelda/typos-<platform> optionalDependency binary and
// propagates its exit code — see CLAUDE.md for the design.
console.error("typos: not implemented yet");
process.exit(1);
