// Package main is a placeholder entry point. The actual PostToolUse hook
// logic (config discovery, glob matching, command execution) is implemented
// in a later phase — see CLAUDE.md.
package main

func main() {}
