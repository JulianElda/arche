@AGENTS.md

<!--
Claude Code reads CLAUDE.md, not AGENTS.md, so this file imports it. Keep the
portable baseline in AGENTS.md so other agents read the same conventions.

Language- and framework-specific rules live in .claude/rules/, path-scoped by
file extension so they enter context only when a matching file does.
-->
